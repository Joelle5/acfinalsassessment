hr
h1
 |Note Editor
 = link_to 'Log Out', destroy_user_session_path, method: :delete, :class => "btn btn-outline-danger"
.container
  h3= @note.title
  .container
  = simple_form_for @note, url: users_note_path, method: :put do |f|
    = f.input :title, label: false
    = f.input :body, label: false
    = f.button :submit, :label =>"Save", :class => "btn btn-outline-info"
    - if flash[:notice]
      a.notice
        |  =
        = flash[:notice]
        |  =
  p
  h4= link_to 'Back to your notes', users_notes_path
